/**
 * Global configuration variables for the frontend application.
 */
export const CONFIG = {
  // Placeholder URL for the GitHub repository.
  // Replace this with the actual repository URL when available.
  GITHUB_REPO_URL: 'https://github.com/openkku/cp-examseat',
};
